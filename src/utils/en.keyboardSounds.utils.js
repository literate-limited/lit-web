// Minimal English keyboard layout (no hover sounds).
// <Keyboard /> supports optional Audio instances per key; this provides the same shape without audio.

import qwerty from './qwertyEnglish.utils.js';

export default qwerty;

